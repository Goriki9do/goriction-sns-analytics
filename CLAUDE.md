# CLAUDE.md

## 作業前に必ず確認すること

このリポジトリ(goriction-sns-analytics)で何か作業する前に、以下のNotionページを必ず確認すること。
過去の経緯・失敗例・方針が記載されており、同じ失敗を繰り返さないための引き継ぎ資料になっている。

- ページ: 「🛠️ ゴリクション自作SNS分析｜YouTube構築記録と他SNSへの展開手順」
- URL: https://app.notion.com/p/3e528d936de381a7b571d21a67f2b4e2

## 作業後に必ず記録すること

意味のある作業（新機能の実装、実データでの動作確認、バグ修正、設定変更、次回への申し送り事項など）を行ったら、
指示されなくても上記Notionページに追記すること。特に以下は必ず記録する。

- 何を実装/変更したか、なぜそうしたか
- 実データでの検証結果（成功・失敗どちらも）
- ハマった点とその解決方法（再発防止のため）
- 未解決の課題・次回やること

雑談や調査だけで実装に至らなかった場合は無理に書かなくてよい。迷ったら書く。

## 方針

- 書き込み系操作（投稿・削除・公開設定変更など）は実装しない（取得のみ）。
- 課金、既存設定の大幅変更、外部公開、認証情報の外部送信は実施直前に本人確認する。
- コメントは「件数」ではなく、返信を含めた本人除外後の「ユニーク投稿者数」を主指標とする。
  取得できない場合は「正確な人数」と表示せず、不明／部分取得として扱う。
- 認証情報・実データ（`secrets/`・`exports/`・`data/`等）はコミットしない。
